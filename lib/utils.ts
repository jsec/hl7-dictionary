function getSegmentDefinition(segmentName: string, version: string): SegmentDefinition {
    return null;
}

function getFieldDefinition(fieldName: string, version: string): FieldDefinition {
    return null;
}

function getMessageDefinition(messageType: string, version: string): MessageDefinition {
    return null;
}
